% Add folders to path.

close all;clear all;clc


addpath(pwd);

cd Compt/;
addpath(genpath(pwd));
cd ..;

cd evaluation/;
addpath(genpath(pwd));
cd ..;

cd dataset/;
addpath(genpath(pwd));
cd ..;

